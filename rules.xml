<ruleset
	xmlns="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://mojo.codehaus.org/versions-maven-plugin/rule/2.0.0 http://mojo.codehaus.org/versions-maven-plugin/xsd/rule-2.0.0.xsd">

	<rules>
		<rule groupId="org.hibernate">
			<ignoreVersions>
				<ignoreVersion type="regex">.*\.Beta\d</ignoreVersion>
				<ignoreVersion type="regex">.*\.Alpha\d</ignoreVersion>
			</ignoreVersions>
		</rule>
		
		<rule groupId="com.google.guava">
			<ignoreVersions>
				<ignoreVersion type="regex">.*\.r\d+</ignoreVersion>
				<ignoreVersion type="regex">.*-rc\d+</ignoreVersion>
			</ignoreVersions>
		</rule>

		<rule groupId="commons-collections" artifactId="commons-collections">
			<ignoreVersions>
				<ignoreVersion>20040616</ignoreVersion>
				<ignoreVersion>20030418.083655</ignoreVersion>
				<ignoreVersion>20031027.000000</ignoreVersion>
				<ignoreVersion>20040102.233541</ignoreVersion>
			</ignoreVersions>
		</rule>

		<rule groupId="javax.mail">
			<ignoreVersions>
				<ignoreVersion type="regex">.*-b\d+</ignoreVersion>
				<ignoreVersion type="regex">.*-rc\d+</ignoreVersion>
			</ignoreVersions>
		</rule>

		<rule groupId="javax.servlet">
			<ignoreVersions>
				<ignoreVersion type="regex">.*-alpha-\d+</ignoreVersion>
			</ignoreVersions>
		</rule>

		<rule groupId="javax.validation">
			<ignoreVersions>
				<ignoreVersion type="regex">.*\.Alpha\d+</ignoreVersion>
				<ignoreVersion type="regex">.*\.Beta\d+</ignoreVersion>
			</ignoreVersions>
		</rule>
		
		<rule groupId="jaxen">
			<ignoreVersions>
				<ignoreVersion type="regex">.*-beta-\d+</ignoreVersion>
				<ignoreVersion>full</ignoreVersion>
			</ignoreVersions>
		</rule>
		
		<rule groupId="org.jadira.*">
			<ignoreVersions>
				<ignoreVersion type="regex">.*\.CR\d+</ignoreVersion>
			</ignoreVersions>
		</rule>
		
		<rule groupId="org.apache.*">
			<ignoreVersions>
				<ignoreVersion type="regex">.*-alpha\d+</ignoreVersion>
				<ignoreVersion type="regex">.*-beta\d+</ignoreVersion>
			</ignoreVersions>
		</rule>
		
		<rule groupId="org.jasig.cas">
			<ignoreVersions>
				<ignoreVersion type="regex">.*-RC\d+</ignoreVersion>
			</ignoreVersions>
		</rule>
		
		<rule groupId="org.mockito">
			<ignoreVersions>
				<ignoreVersion type="regex">.*-rc\d+</ignoreVersion>
			</ignoreVersions>
		</rule>
		
		<rule groupId="org.slf4j">
			<ignoreVersions>
				<ignoreVersion type="regex">.*\.RC\d+</ignoreVersion>
				<ignoreVersion type="regex">.*-RC\d+</ignoreVersion>
				<ignoreVersion type="regex">.*-M\d+</ignoreVersion>
				<ignoreVersion type="regex">.*-alpha\d+</ignoreVersion>
				<ignoreVersion type="regex">.*-beta\d+</ignoreVersion>
			</ignoreVersions>
		</rule>
		
		<rule groupId="xml-apis">
			<ignoreVersions>
				<ignoreVersion>2.0.2</ignoreVersion>
				<ignoreVersion>2.0.0</ignoreVersion>
			</ignoreVersions>
		</rule>
		
		<rule groupId="org.hamcrest">
			<ignoreVersions>
				<ignoreVersion type="regex">.*\.RC\d+</ignoreVersion>
			</ignoreVersions>
		</rule>
		
		<rule groupId="org.hibernate.javax.persistence" artifactId="hibernate-jpa-2.1-api">
			<ignoreVersions>
				<ignoreVersion type="regex">.*\.Draft-.*</ignoreVersion>
			</ignoreVersions>
		</rule>
		
		<rule groupId="org.hibernate.javax.persistence"
			artifactId="hibernate-jpa-2.0-api">
			<ignoreVersions>
				<ignoreVersion type="regex">.*-CR-\d+</ignoreVersion>
			</ignoreVersions>
		</rule>
		
	</rules>
</ruleset>
